import { Router } from "express";
import { upload } from "../middlewares/multer.middlewares";

const router = Router();
// router.route("/dash-board").get(verifyJWT, getChannelStats);

export default router;
